# Kubernetes resources necessary for deploying the SOA implementation

## Deploy
Run `kubectl apply -f deps/.` and then `kubectl apply -f app/.`
Make sure your cluster admin has created a PersistentVolume for you to use